#include "quat.h"
#include "functions.h"

int main ( void ) {

	ofstream myfile;
	/*
	int dim = 3;
	// Allocate space for the matrix and the vectors
	Comp **a = new Comp* [dim];
	for(int i = 0; i < dim; i++ )
		a[i] = new Comp [dim];
	
	Comp *b = new Comp [dim];
	Comp *c = new Comp [dim];

	a[0][0] = Comp( 0., 0. ); a[0][1] = Comp( 2., 0. );  a[0][2] = Comp( 3., 0. );
	a[1][0] = Comp( 1., 0. ); a[1][1] = Comp( 20., 0. ); a[1][2] = Comp( 4., 0. ); 
	a[2][0] = Comp( 0., 0. ); a[2][1] = Comp( 1., 0. );  a[2][2] = Comp( 5., 0. );  

	b[0] = Comp( 1., 0. );
	b[1] = Comp( 1., 0. );
	b[2] = Comp( 1., 0. );

	myfile.open ("example.txt" );
	if ( powerandwrite( a, b, c, dim, myfile ) == 0 )
		cout << "method converged" << endl; 
	else
		cout << "method failed to converge" << endl;
	myfile.close();
*/
		
	
	return 0;
}
